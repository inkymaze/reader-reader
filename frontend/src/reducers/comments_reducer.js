import merge from 'lodash/merge';




const CommentsReducer = (state = {}, action) => {
  switch (action.type) {
    case :
      return merge()
    case :

    default:
      return state;

  }
};

export default CommentsReducer;
